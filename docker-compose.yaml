version: "3.8"

services:
  feedreader:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - feedreader_data:/data
    environment:
      - DATABASE_PATH=/data/feedreader.db
    restart: unless-stopped

volumes:
  feedreader_data:
